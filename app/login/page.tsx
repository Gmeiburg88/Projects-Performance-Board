import { loginAction } from "@/server-actions/dashboard";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const params = await searchParams; const hasError = params?.error === "1"; return <div className="min-h-screen grid place-items-center bg-slate-50 p-4"><div className="w-full max-w-sm rounded-3xl bg-white p-6 shadow-sm border border-slate-200"><h1 className="text-2xl font-semibold text-slate-900">Admin login</h1><p className="mt-1 text-sm text-slate-500">Local login for the dashboard admin area.</p><form action={loginAction} className="mt-4 space-y-3"><input name="username" className="w-full rounded-xl border px-3 py-2" placeholder="Username" /><input name="password" className="w-full rounded-xl border px-3 py-2" type="password" placeholder="Password" />{hasError ? <p className="text-sm text-red-600">Invalid username or password.</p> : null}<button className="w-full rounded-xl bg-slate-900 px-4 py-2 text-white">Sign in</button></form></div></div>; }
